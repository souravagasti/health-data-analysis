import pandas as pd

def build_features(steps: pd.DataFrame, sleep: pd.DataFrame, hr: pd.DataFrame) -> pd.DataFrame:
    """Stub: join steps, sleep, hr into a feature table."""
    return pd.DataFrame()
